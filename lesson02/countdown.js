// Create a single loop that console logs a variable number (✔️)
// Decrease the variable by one each loop (✔️)
// Stop once log outputs 0 (✔️)

let x = 15;

while (x >= 0) {
  console.log(x);
  x--;
}